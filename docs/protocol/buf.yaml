version: v2
modules:
  - path: .
lint:
  use:
    - STANDARD
  except:
    # Public semantic result types are intentionally shared by equivalent
    # mutations, and accepted/event envelope names describe their semantics.
    - RPC_REQUEST_RESPONSE_UNIQUE
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
